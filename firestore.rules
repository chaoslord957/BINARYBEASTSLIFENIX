/**
 * @fileoverview Firestore Security Rules for the Medical Vault application.
 *
 * Core Philosophy:
 * This ruleset allows public read and write access to the medicalDocuments collection for demonstration purposes.
 * In a production app, this would be replaced with a strict user-ownership model.
 *
 * Data Structure:
 * All medical documents are stored in the /medicalDocuments/{documentId} path.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to medical documents.
     * @path /medicalDocuments/{documentId}
     * @allow (read, write) - Allows public read and write access for the demo.
     * @principle Open access for demonstration.
     */
    match /medicalDocuments/{documentId} {
      // WARNING: This allows anyone to read, write, and delete documents.
      // This is for demonstration purposes only.
      // In a real application, you would implement proper user authentication and authorization.
      allow read, write: if true;
    }
  }
}
